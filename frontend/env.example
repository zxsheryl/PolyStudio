# 前端环境变量示例（可选）
#
# 开发模式下本项目使用 Vite proxy（/api、/storage）转发到后端，通常不需要前端 .env。
# 如果你要部署到“前后端不同域名/端口、且没有反向代理”的环境，可考虑引入这些变量，
# 并在前端请求中拼接基地址（需要配合少量代码改动）。
#
# 复制命令：
#   cd 001-生图Agent_new/frontend
#   cp env.example .env

# VITE_API_BASE=http://localhost:8000
# VITE_STORAGE_BASE=http://localhost:8000



